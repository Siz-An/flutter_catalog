import 'package:flutter/cupertino.dart';

class statful extends StatelessWidget {
  const statful({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
